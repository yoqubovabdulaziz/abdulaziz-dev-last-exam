import React, { memo } from 'react'
import "./Admin.scss"

const Admin = () => {
    return (
        <section id="admin">
            <div className="container admin">
                <h1>admin</h1>
            </div>
        </section>
    )
}

export default memo(Admin)